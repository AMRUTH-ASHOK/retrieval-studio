{
  "name": "retrieval-studio",
  "version": "1.0.0",
  "private": true,
  "description": "RAG Pipeline Data Preparation and Evaluation Platform",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
